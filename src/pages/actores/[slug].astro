---
import ProcessCard from '../../components/ProcessCard.astro';
import SiteLayout from '../../layouts/SiteLayout.astro';
import {
  catalog,
  editorialPreviewEnabled,
  processesForActor,
} from '../../lib/data';
import type { CatalogItem } from '../../lib/types';

export function getStaticPaths() {
  return catalog('actores')
    .filter((item) => processesForActor(item.id).length > 0)
    .map((item) => ({
      params: { slug: item.slug },
      props: { item },
    }));
}

interface Props {
  item: CatalogItem;
}

const { item } = Astro.props;
const related = processesForActor(item.id);
---

<SiteLayout title={item.nombre} description={`Procesos vinculados con ${item.nombre}.`}>
  <div class="page-shell">
    <header class="page-intro">
      <div>
        <p class="eyebrow">Actor</p>
        <h1>{item.nombre}</h1>
      </div>
      <p>{related.length} procesos del Observatorio vinculados con este actor.</p>
    </header>
    <section class="section-shell">
      <div class="process-grid">
        {related.map((process) => (
          <ProcessCard process={process} editorialPreview={editorialPreviewEnabled} />
        ))}
      </div>
    </section>
  </div>
</SiteLayout>
